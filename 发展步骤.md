你现在最适合走的是“分层补齐”路线，不是“零散补功能”。因为你们项目已经有前端、IR、RISC-V 后端雏形了，真正卡住你继续接近对方项目的，是中间缺了一整层“标准化 IR + Pass 流水线”。

**先说结论**
你和对方项目最大的差距，不是语法前端，也不是寄存器分配，而是这三件事还没成体系：
- 没有独立的 `PassManager/Pass` 基础设施，优化还主要揉在 IR 构建或后端里
- IR 虽然有 SSA/`phi` 的影子，但还没有完整的 `phi` 消解、CFG 规范化、`Mem2Reg` 这类中间层
- 后端已经能出汇编，但很多问题其实是“前面没清干净”，导致后端被迫硬吃原始 IR

所以最优起点不是“去学 ARM 后端”或“补更多指令”，而是先把你们的 IR 变成一个能被 pass 处理的中端平台。

**你当前项目的真实位置**
你现在已经完成了这些：
- 驱动能跑通前端到后端的主链路，[sysy_compiler.cpp](/d:/Compiler_Code/Compiler-Competition-2025/tools/sysy_compiler.cpp#L29) 到 [sysy_compiler.cpp](/d:/Compiler_Code/Compiler-Competition-2025/tools/sysy_compiler.cpp#L43)、[sysy_compiler.cpp](/d:/Compiler_Code/Compiler-Competition-2025/tools/sysy_compiler.cpp#L109)
- IRBuilder 已经有“建 IR 时顺手做常量折叠”的能力，[IRBuilder.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/IR/IRBuilder.cpp#L72)、[constantfolder.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/IR/constantfolder.cpp#L44)
- 后端已经有活跃变量分析、冲突图、图着色、spill 重写，[AsmFunction.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/backend/AsmFunction.cpp#L78) 到 [AsmFunction.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/backend/AsmFunction.cpp#L235)

这说明你不是“从零开始”，而是“骨架齐了，但中端没有长出来”。

**现在最明显的代码断点**
这些地方是我认为最值得优先修的具体问题：

- `phi` 已定义，但后端直接忽略了它。  
  [instruction.h](/d:/Compiler_Code/Compiler-Competition-2025/include/ir/Value/instruction.h#L138) 定义了 `Phi`，但 [AsmBasicBlock.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/backend/AsmBasicBlock.cpp#L104) 到 [AsmBasicBlock.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/backend/AsmBasicBlock.cpp#L106) 直接 `return nullptr;`。  
  这会让你们 IR 能表达的东西比后端能消费的东西更强，属于结构断层。

- 后端里甚至已经留了 phi 处理草稿，但没有真正接上。  
  [component.h](/d:/Compiler_Code/Compiler-Competition-2025/include/backend/component.h#L255) 到 [component.h](/d:/Compiler_Code/Compiler-Competition-2025/include/backend/component.h#L271) 这段注释说明你们自己也意识到了这个问题。

- 当前优化几乎都还在 IRBuilder 的“建的时候顺手折叠”。  
  [IRBuilder.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/IR/IRBuilder.cpp#L4) 到 [IRBuilder.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/IR/IRBuilder.cpp#L174) 这种模式对简单常量传播很好，但它替代不了独立 pass。你没法系统地做 `DCE`、`SimplifyCFG`、`Mem2Reg`、`GVN`。

- `Insert()` 还是空实现，说明 IRBuilder 的插入点语义还不完整。  
  [IRBuilder.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/IR/IRBuilder.cpp#L224) 到 [IRBuilder.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/IR/IRBuilder.cpp#L233)

- `Module`/`BasicBlock`/`Instruction` 层目前更像数据结构，还没长出系统遍历和变换接口。  
  例如 [module.h](/d:/Compiler_Code/Compiler-Competition-2025/include/ir/module.h#L7)、[basicblock.h](/d:/Compiler_Code/Compiler-Competition-2025/include/ir/basicblock.h#L14)、[instruction.h](/d:/Compiler_Code/Compiler-Competition-2025/include/ir/Value/instruction.h#L40)

- `SymbolTable::insert()` 里还带强制打印。  
  [Visitor.h](/d:/Compiler_Code/Compiler-Competition-2025/include/GenIR/Visitor.h#L85) 到 [Visitor.h](/d:/Compiler_Code/Compiler-Competition-2025/include/GenIR/Visitor.h#L118)  
  这不是性能核心问题，但说明前端还处在“调试态”，会影响批量测试和输出干净度。

**推荐你按这个顺序对齐**

**第一阶段：把 IR 变成“可优化”的平台**
目标：先别追求强优化，先把 pass 框架立住。

1. 新建 `Pass` 基础设施  
建议目录直接对齐你现有结构，新建：
- `include/ir/opt/Pass.h`
- `include/ir/opt/PassManager.h`
- `lib/IR/Pass.cpp`
- `lib/IR/PassManager.cpp`

最小接口就够：
- `class Pass { virtual bool runOnModule(Module&) = 0; }`
- 如果你想更细，也可以有 `FunctionPass`、`BasicBlockPass`
- `PassManager` 只需要维护一个 pass 列表，顺序执行，支持打印前后 IR

这一步的意义：
- 以后所有优化都不再塞进 `Visitor`、`IRBuilder`、后端转换里
- 你开始拥有和对方项目相同的“流水线思维”

2. 在驱动里插入 pass 阶段  
改 [sysy_compiler.cpp](/d:/Compiler_Code/Compiler-Competition-2025/tools/sysy_compiler.cpp#L100) 附近，变成：
- 先 build module
- 再 run `PassManager`
- 再 emit IR 或 asm

也就是说逻辑从：
`source -> module -> asm`
变成：
`source -> module -> optimize/lower -> asm`

3. 先补遍历辅助接口  
为了写 pass，建议先给这些结构补几个顺手接口：
- `Module` 提供遍历函数列表、全局列表的 const/non-const 访问
- `BasicBlock` 提供删除指令、拿终结指令、判断空块等辅助
- `Instruction` 提供替换 use、删除自己、判断副作用等基础方法

如果没有这些接口，你后面每个 pass 都会特别痛苦。

**第二阶段：第一批必须做的 3 个 pass**
这是最关键的一段。不要一上来追 `GVN/LICM`，先做这三个。

1. `SimplifyCFG`
建议文件：
- `include/ir/opt/SimplifyCFG.h`
- `lib/IR/SimplifyCFG.cpp`

最小目标：
- 删除条件恒真的分支
- 删除条件恒假的分支
- 合并只有一个前驱/后继的空跳转块
- 删除不可达块

依赖你已有的：
- `BasicBlock::getPredBlock()` / `getSuccBlock()`，[basicblock.h](/d:/Compiler_Code/Compiler-Competition-2025/include/ir/basicblock.h#L55)
- `removeEntryFromPhi()`，[basicblock.h](/d:/Compiler_Code/Compiler-Competition-2025/include/ir/basicblock.h#L88)

这是最先做的原因：
- 实现难度低
- 对后续 `Mem2Reg` 和 `phi` 处理帮助很大
- 对 if/while 生成的脏 CFG 有立刻收益

2. `Mem2Reg`
建议文件：
- `include/ir/opt/Mem2Reg.h`
- `lib/IR/Mem2Reg.cpp`

最小版不要一步做到完整 LLVM 风格支配前沿插 `phi`。你可以先做“局部/半局部版”：
- 只提升函数内、地址不逃逸、只被 `load/store` 使用的标量 `alloca`
- 如果变量只在单基本块内使用，直接消掉 `load/store`
- 如果是简单 if/while 场景，再逐步引入 `phi`

你们现在的 IR 生成很依赖 `alloca + load/store`，比如局部变量初始化都这么干，[Visitor.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/IR/GenIR/Visitor.cpp#L276) 到 [Visitor.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/IR/GenIR/Visitor.cpp#L282)。  
所以 `Mem2Reg` 是你从“能编译”走向“像编译器”的分水岭。

3. `DCE`
建议文件：
- `include/ir/opt/DCE.h`
- `lib/IR/DCE.cpp`

最小逻辑：
- 遍历每个指令
- 没有 use 且无副作用的指令删除
- 对 `store`、`call`、`br`、`ret` 默认视为有副作用
- 在 `SimplifyCFG` 和 `Mem2Reg` 后反复跑

为什么这一步很值：
- `Mem2Reg` 和常量折叠之后会产生大量死指令
- 不做 `DCE`，你后端看到的还是很多垃圾 IR

**第三阶段：把 SSA 和后端接上**
这是你和对方项目目前最结构性的差距。

1. 明确策略：`phi` 不在后端处理，应该在后端前消解  
你现在的做法是 IR 有 `phi`，后端装作没看见，[AsmBasicBlock.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/backend/AsmBasicBlock.cpp#L104)。  
更合理的路线是：
- 中端允许 SSA / `phi`
- 在进入后端前跑一个 `LowerPhi` 或 `RegAllocPrep` pass
- 把 `phi` 转成前驱块末尾的 `move`

建议文件：
- `include/ir/opt/LowerPhi.h`
- `lib/IR/LowerPhi.cpp`

最小逻辑：
- 对每个带 `phi` 的块
- 对每个前驱块插入并行 move
- 如果前驱有多个后继且目标块有多个前驱，先拆 critical edge
- 最后删 `phi`

2. 然后再让后端只处理“无 phi 的普通 CFG IR”
这样 [AsmBasicBlock.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/backend/AsmBasicBlock.cpp#L68) 这层才会更稳定。  
后端不应该替你承担 SSA 语义恢复工作。

**第四阶段：补最有收益的中端优化**
等前面三层站住之后，再按这个顺序补：

1. `Dominators`
- 为 `Mem2Reg` 完整版和后续 `LICM` 打基础

2. `GVN` 或更简单的 `CSE`
- 先做块内公共子表达式消除都值

3. `Load/Store` 简化
- 例如同地址连续 load/store 的局部优化

4. `InstCombine`
- 把 `add x, 0`、`mul x, 1`、重复 cast 等简化掉

5. `LoopSimplify`
- 规范循环头、latch、preheader

这时候你和对方的差距就会快速缩小，因为你已经从“单次翻译器”进入“优化编译器”的轨道了。

**后端这边你应该怎么排优先级**
你们后端不是最先要补的，但有几件事值得排上。

1. 先把 calling convention 和栈帧规则再系统检查一遍  
因为 [AsmFunction.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/backend/AsmFunction.cpp#L165) 到 [AsmFunction.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/backend/AsmFunction.cpp#L176) 当前可分配物理寄存器集合比较简化，测试也在刻意避开 `s0` 等寄存器，[test_backend.cpp](/d:/Compiler_Code/Compiler-Competition-2025/tests/test_backend.cpp#L82)。  
这更像“先跑起来”的分配器，不是成熟 ABI 级别实现。

2. `GEP`/数组寻址建议晚一点精修  
因为中端没规范之前，后端地址计算会一直很乱。  
[AsmBasicBlock.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/backend/AsmBasicBlock.cpp#L187) 到 [AsmBasicBlock.cpp](/d:/Compiler_Code/Compiler-Competition-2025/lib/backend/AsmBasicBlock.cpp#L241) 这里先别着急做复杂优化。

3. 补 peephole 放在 `LowerPhi` 之后  
比如：
- `addi x, x, 0` 删掉
- `mv` 连锁合并
- 分支反转减少跳转

**测试也要升级，不然你会越改越心虚**
你现在测试偏“冒烟测试”：
- [test_driver.cpp](/d:/Compiler_Code/Compiler-Competition-2025/tests/test_driver.cpp#L35)
- [test_backend.cpp](/d:/Compiler_Code/Compiler-Competition-2025/tests/test_backend.cpp#L49)

建议分三层补：

1. IR pass 单测  
输入一段构造好的 IR，跑 pass，断言输出 IR 是否符合预期。  
这类测试最重要，因为 `Mem2Reg/SimplifyCFG/DCE` 都应该独立验证。

2. 端到端功能测试  
给 `.sy`，比对返回值或输出。

3. 后端结构测试  
像你现在这样查关键汇编片段可以保留，但别把它当主验证方式。

**最实际的 4 周路线**
如果你现在就开始对齐，我建议按这个节奏：

1. 第 1 周  
完成 `Pass/PassManager`，驱动接入 pass 流水线，去掉 `SymbolTable::insert()` 的调试打印，[Visitor.h](/d:/Compiler_Code/Compiler-Competition-2025/include/GenIR/Visitor.h#L85)

2. 第 2 周  
实现 `SimplifyCFG` + 基础 `DCE`，并补 IR 级单测

3. 第 3 周  
实现最小版 `Mem2Reg`，先覆盖单基本块和简单 if 场景

4. 第 4 周  
实现 `LowerPhi`，把后端前 `phi` 全部消掉，然后清理后端里对 `phi` 的空处理逻辑

**如果让我只给一个“第一刀”**
第一刀就做这个：

- 新建 `PassManager`
- 接入 `sysy_compiler`
- 写 `SimplifyCFG`
- 写 `DCE`

因为这一步最稳，改动最可控，而且做完以后你整个项目的开发方式会升级。之后 `Mem2Reg` 才有地方放，`LowerPhi` 才顺。

如果你愿意，我下一步可以直接继续帮你做一件更具体的事：
1. 按你当前仓库结构，把 `PassManager + Pass` 的文件骨架直接设计出来
2. 继续细化 `SimplifyCFG` 的实现方案，具体到要改哪些类、加哪些接口、先写哪几个测试